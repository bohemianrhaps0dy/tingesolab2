server:
  port: ${SERVER_PORT}
spring:
  application:
    name: m2
eureka:
  instance:
    preferIPAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka:8761/eureka/